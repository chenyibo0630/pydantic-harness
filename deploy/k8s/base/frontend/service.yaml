apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: pydantic-harness
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: pydantic-harness
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
